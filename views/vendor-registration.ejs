<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Vendor Registration - Siddhi Infonet</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">

  <style>
    * {
      margin: 0; padding: 0; box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      background: #f5f6fa;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      width: 100%;
      max-width: 480px;
      background: #fff;
      padding: 40px 30px;
      border-radius: 12px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.08);
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      from {opacity: 0; transform: translateY(20px);}
      to {opacity: 1; transform: translateY(0);}
    }

    .logo {
      text-align: center;
      margin-bottom: 20px;
    }
    img.logo { height: 80px; width: auto; display: block; }

    h1 {
      text-align: center;
      color: #333;
      font-size: 1.7rem;
      font-weight: 600;
      margin-bottom: 25px;
    }

    .input-group {
      position: relative;
      margin: 18px 0;
    }

    input, textarea {
      width: 100%;
      padding: 14px 12px;
      border: 1.5px solid #ccc;
      border-radius: 8px;
      font-size: 0.95rem;
      outline: none;
      transition: all 0.3s ease;
    }

    input:focus, textarea:focus {
      border-color: #800000;
      box-shadow: 0 0 0 3px rgba(128,0,0,0.1);
    }

    label {
      position: absolute;
      top: 14px;
      left: 14px;
      color: #777;
      font-size: 0.9rem;
      pointer-events: none;
      transition: 0.2s ease all;
      background: #fff;
      padding: 0 4px;
    }

    input:focus + label,
    input:not(:placeholder-shown) + label,
    textarea:focus + label,
    textarea:not(:placeholder-shown) + label {
      top: -8px;
      left: 10px;
      font-size: 0.75rem;
      color: #800000;
    }

    button {
      width: 100%;
      padding: 14px;
      margin-top: 20px;
      border: none;
      border-radius: 8px;
      background: #800000;
      color: #fff;
      font-weight: 600;
      font-size: 1rem;
      cursor: pointer;
      transition: 0.3s ease;
    }

    button:hover {
      background: #a00000;
      transform: translateY(-2px);
    }

    .note {
      margin-top: 18px;
      text-align: center;
      font-size: 0.9rem;
      color: #666;
    }

    @media (max-width: 576px) {
      .container { padding: 30px 20px; }
      h1 { font-size: 1.5rem; }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">
      <img src="/newlogo.png" alt="Siddhi Infonet Logo" class="logo">
    </div>

    <h1>Vendor Registration</h1>

    <form action="/api/vendors/save/vendor" method="post">
      <div class="input-group">
        <input id="companyName" name="companyName" type="text" placeholder=" " required>
        <label for="companyName">Company Name</label>
      </div>

      <div class="input-group">
        <input id="contactPerson" name="contactPerson" type="text" placeholder=" " required>
        <label for="contactPerson">Person's Name</label>
      </div>

      <div class="input-group">
        <input id="phone" name="phone" type="tel" placeholder=" " required>
        <label for="phone">Contact Number</label>
      </div>

      <div class="input-group">
        <input id="email" name="email" type="email" placeholder=" " required>
        <label for="email">Email Id</label>
      </div>

      <div class="input-group">
        <input id="address" name="address" type="text" placeholder=" " required>
        <label for="address">Location</label>
      </div>

      <div class="input-group">
        <input id="teamSize" name="teamSize" type="number" placeholder=" " required>
        <label for="teamSize">Team Size</label>
      </div>

      <div class="input-group">
        <textarea id="message" name="message" rows="3" placeholder=" "></textarea>
        <label for="message">Any Message</label>
      </div>

      <button type="submit">Register</button>
    </form>

    <p class="note">Please ensure all details are correct before submitting.</p>
  </div>
</body>
</html>
