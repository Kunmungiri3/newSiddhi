<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Vendor - Siddhi Infonet</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body {
      font-family: 'Poppins', Arial, sans-serif;
      background: #f9f9f9;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    .container {
      max-width: 500px;
      width: 90%;
      background: #fff;
      padding: 35px 25px;
      border-radius: 14px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.12);
      animation: fadeIn 0.8s ease-in-out;
      text-align: center;
    }

    /* Logo */
    .logo {
      margin-bottom: 20px;
    }
    .logo img {
      max-width: 140px;
      height: auto;
    }

    h1 {
      color: #800000;
      margin-bottom: 25px;
      font-size: 1.8rem;
      font-weight: 600;
    }

    label {
      font-weight: 500;
      display: block;
      margin-top: 15px;
      color: #333;
      font-size: 0.95rem;
      text-align: left;
    }

    input, textarea {
      width: 100%;
      padding: 12px;
      border: 1px solid #ccc;
      border-radius: 8px;
      margin-top: 8px;
      transition: all 0.3s;
      font-size: 0.95rem;
    }

    input:focus, textarea:focus {
      border-color: #800000;
      outline: none;
      box-shadow: 0 0 6px rgba(128,0,0,0.3);
    }

    button {
      margin-top: 25px;
      padding: 14px;
      background: #800000;
      border: none;
      color: #fff;
      font-weight: 600;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s ease;
      width: 100%;
      font-size: 1rem;
    }

    button:hover {
      background: #a00000;
      transform: translateY(-2px);
    }

    .note {
      margin-top: 20px;
      font-size: 0.9rem;
      color: #666;
    }

    /* Animation */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Responsive tweaks */
    @media (max-width: 576px) {
      .container {
        padding: 25px 18px;
      }
      .logo img {
        max-width: 100px;
      }
      h1 {
        font-size: 1.5rem;
      }
    }

    @media (min-width: 577px) and (max-width: 992px) {
      .logo img {
        max-width: 120px;
      }
      h1 {
        font-size: 1.6rem;
      }
    }

    @media (min-width: 993px) {
      .logo img {
        max-width: 160px;
      }
      h1 {
        font-size: 2rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Logo -->
    <div class="logo">
      <img src="/newlogo.png" alt="Siddhi Infonet Logo" class="logo">
    </div>

    <h1>Edit Vendor</h1>

    <p><a href="/admin/vendors" class="btn btn-secondary">Back to Vendor List</a></p>

    <form action="/admin/vendors/<%= vendor._id %>/edit" method="post">
      <label for="companyName">Company Name</label>
      <input id="companyName" name="companyName" type="text" placeholder="enter company name" value="<%= vendor.companyName %>" required>

      <label> Person's Name </label>
      <input type="text" class="form-control" name="contactPerson" placeholder=" Enter your Name " value="<%= vendor.contactPerson %>" required>

      <label for="phone">Contact Number</label>
      <input id="phone" name="phone" type="tel" placeholder="Enter your contact number" value="<%= vendor.phone %>" autocomplete="tel" required>

      <label for="email">Email Id</label>
      <input id="email" name="email" type="email" placeholder="enter your email" value="<%= vendor.email %>" required>

      <label for="address">Location</label>
      <input id="address" name="address" type="text" placeholder="Enter your location" value="<%= vendor.address %>" autocomplete="address-line1" required>

      <label for="teamSize">Team Size</label>
      <input id="teamSize" name="teamSize" type="number" placeholder="Enter team size" value="<%= vendor.teamSize %>" required>

      <label for="message">Any Message</label>
      <textarea id="message" name="message" placeholder="Enter your message" rows="3"><%= vendor.message || '' %></textarea>

      <button type="submit">Update</button>
    </form>

    <p class="note">Please ensure all details are correct before submitting.</p>
  </div>
</body>
</html>
