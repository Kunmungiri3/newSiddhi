<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Jobseeker - Siddhi Infonet</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      font-family: 'Poppins', Arial, sans-serif;
      background: #f9f9f9;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }
    .container {
      max-width: 500px;
      width: 90%;
      background: #fff;
      padding: 35px 25px;
      border-radius: 14px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.12);
      animation: fadeIn 0.8s ease-in-out;
      text-align: center;
    }
    .logo {
      margin-bottom: 20px;
    }
    .logo img {
      max-width: 140px;
      height: auto;
    }
    h1 {
      color: #800000;
      margin-bottom: 25px;
      font-size: 1.8rem;
      font-weight: 600;
    }
    label {
      font-weight: 500;
      display: block;
      margin-top: 15px;
      color: #333;
      font-size: 0.95rem;
      text-align: left;
    }
    input, textarea {
      width: 100%;
      padding: 12px;
      border: 1px solid #ccc;
      border-radius: 8px;
      margin-top: 8px;
      transition: all 0.3s;
      font-size: 0.95rem;
    }
    input:focus, textarea:focus {
      border-color: #800000;
      outline: none;
      box-shadow: 0 0 6px rgba(128,0,0,0.3);
    }
    button {
      margin-top: 25px;
      padding: 14px;
      background: #800000;
      border: none;
      color: #fff;
      font-weight: 600;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s ease;
      width: 100%;
      font-size: 1rem;
    }
    button:hover {
      background: #a00000;
      transform: translateY(-2px);
    }
    .note {
      margin-top: 20px;
      font-size: 0.9rem;
      color: #666;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">
      <img src="/newlogo.png" alt="Siddhi Infonet Logo" class="logo" />
    </div>
    <h1>Edit Jobseeker</h1>
    <form id="editForm" enctype="multipart/form-data" method="POST" action="/api/jobseekers/edit/<%= jobseeker._id %>">
      <label for="name">Name</label>
      <input id="name" name="name" type="text" value="<%= jobseeker.name %>" required />
      
      <label for="father">Father's Name</label>
      <input id="father" name="father" type="text" value="<%= jobseeker.father %>" required />
      
      <label for="phone">Phone</label>
      <input id="phone" name="phone" type="text" value="<%= jobseeker.phone %>" required />
      
      <label for="email">Email</label>
      <input id="email" name="email" type="email" value="<%= jobseeker.email %>" required />
      
      <label for="dob">DOB</label>
      <input id="dob" name="dob" type="date" value="<%= jobseeker.dob ? new Date(jobseeker.dob).toISOString().split('T')[0] : '' %>" />
      
      <label for="qualification">Qualification</label>
      <input id="qualification" name="qualification" type="text" value="<%= jobseeker.qualification %>" />
      
      <label for="address">Address</label>
      <textarea id="address" name="address"><%= jobseeker.address %></textarea>
      
      <label for="aadhar">Aadhar</label>
      <input id="aadhar" name="aadhar" type="text" value="<%= jobseeker.aadhar %>" />
      
      <label for="cv">CV</label><br />
      <% if (jobseeker.cv) { %>
        <a href="<%= jobseeker.cv %>" target="_blank" class="btn btn-success btn-sm mb-2">View Current CV</a><br />
      <% } %>
      <input id="cv" name="cv" type="file" accept=".pdf,.doc,.docx" />
      
      <button type="submit">Save Changes</button>
    </form>
    <p class="note">Please ensure all details are correct before submitting.</p>
  </div>

  <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
  <script>
    $(function() {
      $('#editForm').submit(function(e) {
        e.preventDefault();
        var formData = new FormData(this);
        $.ajax({
          url: $(this).attr('action'),
          type: 'POST',
          data: formData,
          processData: false,
          contentType: false,
          success: function(res) {
            alert('Jobseeker updated successfully.');
            window.opener.location.reload();
            window.close();
          },
          error: function() {
            alert('Error updating jobseeker.');
          }
        });
      });
    });
  </script>
</body>
</html>
